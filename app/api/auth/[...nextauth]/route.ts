import { NextRequest } from "next/server";

// Assuming you need to define the param structure
const handler = {
  __tag__: "GET",
  __param_position__: "first",
  __param_type__: NextRequest, // Ensure it's the correct type for the app directory
};
